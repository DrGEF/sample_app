<% provide(:title, @user.name) %>

<div class="row">
	<aside class="col-md-4">
		<section>
			<h1 style="margin-top: 15px;">
				<%= gravatar_for(@user) %>
				<%= @user.name %>
			</h1>
		</section>
	</aside>
</div>